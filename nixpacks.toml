[phases.setup]
nixPkgs = ['bun', 'openssl']

[phases.install]
cmds = ['bun install --frozen-lockfile']

[phases.build]
cmds = ['bunx prisma generate', 'bun run build']

[start]
cmd = 'node server.js'

[variables]
NODE_ENV = 'production'